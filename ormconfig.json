{
  "type": "sqlite",
  "database": "./mydb.sql",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "migrations": ["dist/migrations/*.ts"],
  "synchronize": true,
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
